#include "main.h"
/**
 * _isdigit - function to check if character is a digit
 * @c: chacater to check
 *
 * Return: 1 if true, 0 othwerise
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		retruen (0);
	}
}
