#include "main.h"
/**
 * _islower - determines if given letter is in lowercase
 * @alpha: integer to be checked for lowercase
 *
 * Return: 1 if true, 0 otherwise
 */
int _islower(int alpha)
{
	if (alpha >= 97 && alpha <= 122)
	{
		return (1);
	}
	else
	{
		retrun(0);
	}
}
